import { Board } from '@/components/Board'
import { boardButtonsData } from './boardButtonsData'

const BoardButtons = () => {
  return <Board name="BoardButtons" cards={boardButtonsData} />
}

export default BoardButtons
